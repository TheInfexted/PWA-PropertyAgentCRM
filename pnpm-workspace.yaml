# pnpm settings live here as of pnpm 10.26+/11 (no longer in package.json's "pnpm" field).
# Allow the two dependencies that need to run a build/install script.
allowBuilds:
  esbuild: true
  '@parcel/watcher': true
