# dependencies
node_modules

# nuxt / nitro build output
.nuxt
.output
.data
dist

# env
.env
.env.*
!.env.example

# logs & os
*.log
.DS_Store

# superpowers brainstorm scratch (mockups, server state)
.superpowers/

# cloud-sync (iCloud/Dropbox) conflict copies — never commit these
* 2.*
* 2
* 3.*
* 3
